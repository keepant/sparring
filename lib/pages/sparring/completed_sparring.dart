import 'package:flutter/material.dart';

class CompletedSparring extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("completed"),
      ),
    );
  }
}
